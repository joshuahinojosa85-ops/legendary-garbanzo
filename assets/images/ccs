* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}

body {
  background: radial-gradient(circle at top, #050505 0%, #000 100%);
  color: #cfcfcf;
  overflow-x: hidden;
  position: relative;
}

/* Animated circuitry grid */
body::before {
  content: "";
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: repeating-linear-gradient(90deg, rgba(0,255,255,0.05) 0px, rgba(0,255,255,0.05) 1px, transparent 1px, transparent 80px),
              repeating-linear-gradient(0deg, rgba(0,255,255,0.05) 0px, rgba(0,255,255,0.05) 1px, transparent 1px, transparent 80px);
  animation: gridmove 20s linear infinite;
  z-index: 0;
}
@keyframes gridmove {
  from { background-position: 0 0, 0 0; }
  to { background-position: 80px 80px, 80px 80px; }
}

header {
  text-align: center;
  padding: 2rem 1rem;
  background: linear-gradient(90deg, #0b0b0b, #101010);
  box-shadow: 0 0 10px #00ffff20;
  position: relative;
  z-index: 2;
}

header img {
  width: 120px;
  margin-bottom: 1rem;
}

header h1 {
  color: #00ffff;
  font-size: 2.2rem;
  text-shadow: 0 0 10px #00ffff;
}

header nav {
  margin-top: 1rem;
}
header nav a {
  margin: 0 1rem;
  color: #9f9f9f;
  text-decoration: none;
  transition: color 0.3s;
}
header nav a:hover, header nav a.active {
  color: #00ffff;
}

section {
  padding: 3rem 1.5rem;
  max-width: 1000px;
  margin: 0 auto;
  text-align: center;
  position: relative;
  z-index: 2;
}

.service-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 2rem;
  margin-top: 2rem;
}

.service-card {
  background: #111;
  padding: 2rem;
  border-radius: 10px;
  box-shadow: 0 0 10px #00ffff20;
  transition: transform 0.3s, box-shadow 0.3s;
}
.service-card:hover {
  transform: translateY(-10px);
  box-shadow: 0 0 20px #00ffff80;
}
.service-card h3 {
  color: #00ffff;
  margin-bottom: 1rem;
}

.team-grid {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 2rem;
  margin-top: 2rem;
}
.member {
  background: #111;
  padding: 1.5rem;
  border-radius: 10px;
  box-shadow: 0 0 10px #00ffff30;
  width: 250px;
}
.member h3 {
  color: #00ffff;
  margin-bottom: 0.5rem;
}

blockquote {
  font-style: italic;
  color: #a0a0a0;
  margin-top: 3rem;
  border-left: 4px solid #00ffff;
  padding-left: 1rem;
}
blockquote span {
  display: block;
  color: #00ffff;
  margin-top: 0.5rem;
}

footer {
  text-align: center;
  padding: 1rem;
  font-size: 0.9rem;
  color: #666;
  border-top: 1px solid #111;
  background: #0b0b0b;
  position: relative;
  z-index: 2;
}

[data-animate] {
  opacity: 0;
  transform: translateY(20px);
  transition: all 0.8s ease;
}
[data-animate].visible {
  opacity: 1;
  transform: translateY(0);
}
